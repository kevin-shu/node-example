var ElasticSearchClient = require('./index.js')

var serverOptions = {
    host:'localhost',
    port:9200
    //secure: true,
    /*auth: {
     username:'username',
     password:'password'
     }*/
};

var indexName = 'your_index_name';
var objName = 'your_object_name';

var elasticSearchClient = new ElasticSearchClient(serverOptions);

elasticSearchClient.update(indexName, objName, "sushi", {doc:{occupation: "play"}})
    .on('data', function(data) {
        console.log('wtf')
        console.log(data)
    })
    .exec()

//elasticSearchClient.update(indexName, objName, "sushi", {doc:{occupation: "play"}})

//    .exec();
    /*, function (err, data) {
    if(err){
        console.log(err)
    }else{
        console.log(data)
    }*/
//})